# draw square
from os import system
system("cls") 

size   = int(input("size: "))
n      = 1
length = size * size 


print()

while n <= length :
    print("+ ", end="")

    if n % size ==0:
      print()
  
    n += 1

print()
